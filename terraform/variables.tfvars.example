aws_region       = "us-east-1"
instance_type    = "t3.large"
key_pair_name    = "my-jenkins-keypair"     # must already exist in AWS
allowed_ssh_cidr = "0.0.0.0/0"              # replace with your IP/32 for better security
root_volume_size = 30
