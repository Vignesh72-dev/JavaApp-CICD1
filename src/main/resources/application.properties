server.port=8080
spring.application.name=javaapp-cicd
app.version=1.0.0

# Actuator health endpoint (used for post-deploy smoke test in the pipeline)
management.endpoints.web.exposure.include=health,info
